# Bridge
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Triage agent (LangGraph)
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0

# Knowledge agent
# (uses stdlib only for TF-IDF + optional langchain-openai for synthesis)

# Compliance agent (Featherless via httpx — already listed above)

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx  # already listed, also used in tests
